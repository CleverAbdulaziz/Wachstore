package com.wristwatch.shop.bot;

public enum AdminAction {
    NONE,          // default, no action in progress
    ADD_PRODUCT,   // admin is adding a product
    ADD_CATEGORY   // admin is adding a category
}
